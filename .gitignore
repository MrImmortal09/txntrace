# Real bank/card statements dropped at the repo root for parser tuning.
# These carry full PII (name, address, account numbers) — never commit them.
/*.csv
/*.xls
/*.xlsx
/*.pdf

# Secrets, certificates, and signing keys - NEVER COMMIT
*.p12
*.pfx
*.mobileprovision
*.cer
*.crt
*.der
*.pem
*.key
*.keystore
*.jks
*.secret
secrets/

# Environment files
.env
.env.*
*.env

# Credentials & Service Accounts
*credentials*.json
*service-account*.json
google-services.json
GoogleService-Info.plist

# Build outputs
*.ipa
*.aab
*.apk
*.xcarchive
Payload/
