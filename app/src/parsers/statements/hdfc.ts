import { ParsedTransaction } from '../../types';

export const parseStatement = (rawText: string, isCsv: boolean): ParsedTransaction[] => {
  // Skeleton implementation for HDFC
  // In the next phase, we will implement the actual column parsing logic here.
  return [];
};
